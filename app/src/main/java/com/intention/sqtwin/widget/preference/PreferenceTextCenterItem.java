/*
package com.intention.sqtwin.widget.preference;

import android.content.Context;
import android.view.Gravity;
import android.view.ViewGroup;

*/
/**
 * Created by caiying on 07/04/15.
 *//*

public class PreferenceTextCenterItem extends PreferenceAbstractItem {

    public PreferenceTextCenterItem(Context context, String title, OnClickListener onClickListener) {
        this(context, title, context.getResources().getColor(android.R.color.black), onClickListener);
    }

    public PreferenceTextCenterItem(Context context, String title, int color, OnClickListener onClickListener) {
        super(context);
        mTitleTextView.setText(title);
        this.mOnClickListener = onClickListener;
        mTitleTextView.setGravity(Gravity.CENTER);
        mTitleTextView.setTextColor(color);
        LayoutParams params = new LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.MATCH_PARENT);
        mTitleTextView.setLayoutParams(params);
    }

    public void updateTitle(String title) {
        mTitleTextView.setText(title);
    }
}
*/
