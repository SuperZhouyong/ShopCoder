package cn.hancang.www.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import cn.hancang.www.greendao.ProfessionReport;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PROFESSION_REPORT".
*/
public class ProfessionReportDao extends AbstractDao<ProfessionReport, String> {

    public static final String TABLENAME = "PROFESSION_REPORT";

    /**
     * Properties of entity ProfessionReport.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property ProfessionId = new Property(0, String.class, "ProfessionId", true, "PROFESSION_ID");
        public final static Property ProfessionName = new Property(1, String.class, "ProfessionName", false, "PROFESSION_NAME");
    }


    public ProfessionReportDao(DaoConfig config) {
        super(config);
    }
    
    public ProfessionReportDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PROFESSION_REPORT\" (" + //
                "\"PROFESSION_ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: ProfessionId
                "\"PROFESSION_NAME\" TEXT);"); // 1: ProfessionName
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PROFESSION_REPORT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ProfessionReport entity) {
        stmt.clearBindings();
 
        String ProfessionId = entity.getProfessionId();
        if (ProfessionId != null) {
            stmt.bindString(1, ProfessionId);
        }
 
        String ProfessionName = entity.getProfessionName();
        if (ProfessionName != null) {
            stmt.bindString(2, ProfessionName);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ProfessionReport entity) {
        stmt.clearBindings();
 
        String ProfessionId = entity.getProfessionId();
        if (ProfessionId != null) {
            stmt.bindString(1, ProfessionId);
        }
 
        String ProfessionName = entity.getProfessionName();
        if (ProfessionName != null) {
            stmt.bindString(2, ProfessionName);
        }
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public ProfessionReport readEntity(Cursor cursor, int offset) {
        ProfessionReport entity = new ProfessionReport( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // ProfessionId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // ProfessionName
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ProfessionReport entity, int offset) {
        entity.setProfessionId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setProfessionName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final String updateKeyAfterInsert(ProfessionReport entity, long rowId) {
        return entity.getProfessionId();
    }
    
    @Override
    public String getKey(ProfessionReport entity) {
        if(entity != null) {
            return entity.getProfessionId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ProfessionReport entity) {
        return entity.getProfessionId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
